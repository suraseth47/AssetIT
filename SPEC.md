# SPEC: ระบบทะเบียนอุปกรณ์ไอที (Device Registry Web App)

## 1. ชื่อโปรเจคและเป้าหมาย

โปรเจคนี้คือ "ระบบทะเบียนอุปกรณ์ไอที" เว็บแอปที่ทำงานในเบราว์เซอร์ล้วนสำหรับบันทึกและค้นหาข้อมูลอุปกรณ์ไอทีขององค์กรประมาณ 200 เครื่อง (โน้ตบุ๊ก จอ เครื่องพิมพ์ สวิตช์ กล้องวงจรปิด) เพื่อทดแทนการบันทึกด้วยไฟล์ Excel ที่ส่งต่อกันทางอีเมลจนเกิดปัญหาหาข้อมูลไม่เจอ ไม่รู้ว่าเครื่องไหนอยู่กับใคร และมีหลายเวอร์ชันจนสับสนว่าอันไหนล่าสุด เป้าหมายของรอบนี้คือทำให้มีแหล่งข้อมูลกลางเพียงชุดเดียวที่ค้นหาได้เร็ว แก้ไขง่าย และสามารถระบุตัวอุปกรณ์จริงผ่าน QR code ได้ ภายในเวลาพัฒนา 2 วันโดยผู้พัฒนาคนเดียว

## 2. ผู้ใช้และสถานการณ์การใช้งาน

ผู้ใช้งานหลักคือเจ้าของระบบ (เจ้าหน้าที่ไอที) เพียงคนเดียว ไม่มีผู้ใช้อื่นเข้าถึงระบบพร้อมกัน สถานการณ์การใช้งานที่คาดไว้ ได้แก่ การเปิดระบบเพื่อค้นหาอุปกรณ์ด้วยชื่อ รหัสครุภัณฑ์ หรือผู้ถือครอง การเพิ่มอุปกรณ์ใหม่หรือแก้ไขข้อมูลผู้ถือครอง/สถานที่เมื่อมีการโยกย้ายอุปกรณ์ การพิมพ์ QR code ไปติดที่ตัวเครื่องจริง และการสแกน QR code ที่ติดอยู่บนอุปกรณ์เพื่อเปิดดูข้อมูลอย่างรวดเร็วขณะเดินตรวจสอบทรัพย์สินในพื้นที่ทำงานจริง

## 3. ฟีเจอร์ที่ต้องมี

- เพิ่ม แก้ไข ลบ และดูรายการอุปกรณ์ (CRUD) พร้อมบันทึกอัตโนมัติลง localStorage
- ค้นหาแบบพิมพ์คำ (ชื่อ, รหัสครุภัณฑ์, ผู้ถือครอง) และกรองตามประเภท/สถานะ
- นำเข้าข้อมูลจากไฟล์ CSV แบบเลือก map คอลัมน์เอง เพื่อรวมไฟล์ Excel หลายเวอร์ชันเข้าเป็นชุดเดียว
- Export/Import ข้อมูลทั้งหมดเป็นไฟล์ JSON เพื่อสำรองข้อมูล
- สร้างและพิมพ์ QR code ต่ออุปกรณ์ 1 ชิ้น โดยเข้ารหัสรหัสครุภัณฑ์
- พิมพ์ QR code หลายใบพร้อมกันเป็นแผ่นสำหรับติดครุภัณฑ์ทีเดียว
- สแกน QR code ผ่านกล้องเว็บแคมเพื่อเปิดข้อมูลอุปกรณ์นั้นทันที
- หน้าสรุปตัวเลขแบบง่าย แสดงจำนวนอุปกรณ์ตามประเภทและสถานะ
- ไฮไลต์สีเมื่ออุปกรณ์ใกล้หมดประกัน หรือยังไม่ระบุผู้ถือครอง
- แนบรูปถ่ายอุปกรณ์ (บีบอัดขนาดก่อนบันทึก)

## 4. ฟีเจอร์ที่อยู่นอกขอบเขตของรอบนี้

- **ระบบล็อกอิน/หลายผู้ใช้พร้อมกัน** — ผู้ใช้งานจริงมีคนเดียว และไม่มีเซิร์ฟเวอร์ให้ตรวจสอบสิทธิ์
- **ประวัติการเปลี่ยนมือ/ยืม-คืนแบบ log เต็มรูปแบบ** — ตกลงกันแล้วว่าต้องการเก็บแค่สถานะปัจจุบันพอ
- **เชื่อมต่อฐานข้อมูลกลางหรือเซิร์ฟเวอร์ (เช่น Google Sheets API)** — ขัดกับข้อจำกัดของโปรเจกต์ที่ระบุว่าไม่มีเซิร์ฟเวอร์และไม่มีฐานข้อมูล
- **Workflow อนุมัติการโอนย้ายอุปกรณ์ระหว่างแผนก** — เกินความจำเป็นสำหรับผู้ใช้งานคนเดียว
- **รายงานเชิงบริหารแบบ PDF และการคำนวณค่าเสื่อมราคา** — ไม่ใช่ปัญหาเร่งด่วนตามที่ตั้งโจทย์ไว้ในตอนแรก
- **แจ้งเตือนอัตโนมัติผ่านอีเมลหรือ LINE** — ต้องพึ่งพาบริการภายนอกและเซิร์ฟเวอร์ ซึ่งขัดกับข้อจำกัดของระบบ

## 5. โครงสร้างข้อมูลของอุปกรณ์ 1 ชิ้น

| ชื่อช่อง (Field) | ชนิดข้อมูล | ตัวอย่าง |
|---|---|---|
| assetId | string (unique) | "IT-2026-001" |
| category | string (enum: Laptop, Monitor, Printer, Switch, CCTV, Other) | "Laptop" |
| brandModel | string | "Dell Latitude 5420" |
| serialNumber | string | "5CD1234ABC" |
| assignedTo | string | "สมชาย ใจดี / ฝ่ายบัญชี" |
| location | string | "อาคาร A ชั้น 3 ห้อง IT" |
| status | string (enum: InUse, UnderRepair, Spare, Retired) | "InUse" |
| purchaseDate | date (ISO 8601) | "2024-03-15" |
| warrantyExpiry | date (ISO 8601) | "2027-03-15" |
| cost | number | 25000 |
| photo | string (base64 data URL) | "data:image/jpeg;base64,..." |
| notes | string | "จอมีรอยขีดข่วนเล็กน้อย" |
| createdAt | datetime (ISO 8601) | "2026-09-12T10:30:00+07:00" |
| updatedAt | datetime (ISO 8601) | "2026-09-12T14:00:00+07:00" |

## 6. เทคโนโลยีที่ใช้และเหตุผล

- **HTML/CSS/JavaScript ล้วน ทำงานในเบราว์เซอร์โดยไม่ต้องมีเซิร์ฟเวอร์** — ตรงกับข้อจำกัดที่ไม่มีเซิร์ฟเวอร์/ฐานข้อมูล และพัฒนาได้เร็วด้วยผู้พัฒนาคนเดียวภายใน 2 วัน
- **Web Storage API (localStorage)** — เก็บข้อมูลถาวรในเบราว์เซอร์โดยไม่ต้องมีฐานข้อมูลจริง เพียงพอสำหรับผู้ใช้งานคนเดียว
- **ไลบรารีสร้าง QR code (เช่น qrcode.js) โหลดผ่าน CDN** — ไม่ต้องเขียนอัลกอริทึมสร้าง QR เอง ประหยัดเวลาพัฒนา
- **ไลบรารีอ่าน QR จากกล้อง (เช่น html5-qrcode หรือ jsQR)** — รองรับการสแกนผ่านเว็บแคมในเบราว์เซอร์โดยตรง ไม่ต้องติดตั้งแอปเพิ่ม
- **ไลบรารีแปลง CSV (เช่น PapaParse)** — ลดเวลาพัฒนา parser เอง และรองรับไฟล์ที่ export จาก Excel ได้หลากหลายรูปแบบ
- **ไฟล์ JSON สำหรับ export/import ข้อมูล** — เป็นรูปแบบมาตรฐานที่อ่านและเขียนง่ายด้วย JavaScript เหมาะกับการสำรองข้อมูลแบบตรงไปตรงมา

## 7. เกณฑ์ว่างานนี้เสร็จ

- เปิดไฟล์ในเบราว์เซอร์ได้โดยไม่ต้องติดตั้งเซิร์ฟเวอร์หรือเชื่อมต่ออินเทอร์เน็ต (ยกเว้นการโหลดไลบรารีจาก CDN ครั้งแรก)
- เพิ่ม แก้ไข และลบข้อมูลอุปกรณ์ได้ และข้อมูลยังคงอยู่หลังปิดและเปิดเบราว์เซอร์ใหม่
- นำเข้าไฟล์ CSV ที่มีข้อมูลอุปกรณ์ 200 รายการได้สำเร็จโดยไม่มีข้อมูลตกหล่นหรือซ้ำซ้อน
- ค้นหาด้วยรหัสครุภัณฑ์ ชื่อ หรือผู้ถือครอง แล้วได้ผลลัพธ์ที่ถูกต้องภายใน 1 วินาที
- สร้าง QR code ของอุปกรณ์อย่างน้อย 5 ชิ้นทดสอบ แล้วสแกนด้วยกล้องเว็บแคมเปิดข้อมูลตรงตัวได้จริง
- พิมพ์ QR code หลายใบพร้อมกันได้ผลลัพธ์ที่พร้อมตัดไปติดอุปกรณ์จริง
- Export ข้อมูลเป็น JSON แล้วนำกลับมา Import ในเบราว์เซอร์หรือเครื่องอื่นได้ข้อมูลครบถ้วนเหมือนเดิม
- อุปกรณ์ที่ใกล้หมดประกันภายใน 30 วันถูกไฮไลต์สีในหน้ารายการโดยอัตโนมัติ
- แดชบอร์ดแสดงจำนวนอุปกรณ์แยกตามประเภทและสถานะได้ตรงกับข้อมูลจริงที่บันทึกไว้

## 8. ข้อจำกัดที่รู้ตัวอยู่แล้ว

- ข้อมูลเก็บอยู่ในเบราว์เซอร์เครื่องเดียวเท่านั้น หากล้างแคชหรือเปลี่ยนเครื่องโดยไม่ export backup ก่อน ข้อมูลจะสูญหาย
- ไม่รองรับการแก้ไขพร้อมกันจากหลายผู้ใช้ หากต้องแชร์ให้คนอื่นใช้ ต้องส่งไฟล์ export/import ต่อกันเอง ซึ่งยังมีความเสี่ยงเรื่องเวอร์ชันคล้ายปัญหาเดิม แต่ในระดับที่น้อยลง
- localStorage มีขีดจำกัดพื้นที่ประมาณ 5-10MB ต่อโดเมน หากแนบรูปถ่ายจำนวนมากอาจทำให้พื้นที่เต็มเร็วกว่าที่คาด
- ไม่มีระบบยืนยันตัวตน ผู้ใดก็ตามที่เปิดเครื่องหรือเบราว์เซอร์นี้ได้สามารถแก้ไขข้อมูลได้ทั้งหมด
- ไม่มีการเก็บประวัติการแก้ไข หากกรอกข้อมูลผิดพลาดจะไม่สามารถย้อนดูค่าก่อนหน้าได้
- ฟีเจอร์สแกน QR code ผ่านกล้องต้องใช้เบราว์เซอร์ที่รองรับ Camera API/WebRTC และต้องอนุญาตสิทธิ์การเข้าถึงกล้องก่อนใช้งาน
